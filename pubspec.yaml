name: cinema_subtitles
description: Offline subtitle playback clock for cinema use.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.13.0

dependencies:
  crypto: 3.0.7
  file_selector: 1.1.0
  flutter:
    sdk: flutter
  path_provider: 2.1.6
  shared_preferences: 2.5.5
  subtitle: 0.2.0
  wakelock_plus: 1.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
